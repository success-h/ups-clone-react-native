import { Text, View } from "react-native";

interface OrderScreenProps {}

export const OrderScreen = ({}: OrderScreenProps) => {
  return (
    <View>
      <Text className="text-blue-700">My Order screen</Text>
    </View>
  );
};
